extends ../shared/template
block content
  .row
    .col-xs-12
  .row
    .col-xs-4
      a.thumbnail(href='#')
        img(src=user.photo)
    .col-xs-8
      h2: a(href='/profile/edit')= user.displayName || user.email || user.username 
      h3 Email: #{user.email || 'Email not entered yet'}
      h3 Age: #{user.age}

block scripts
